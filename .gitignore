node_modules
package-lock.json
dist
.vite

# Android signing — the keystore is the app's identity; never commit it.
*.keystore
*.jks
*.keystore.b64
.DS_Store
*.log
.env
.env.local
.vs/
.claude/

# Capacitor — android/ folder is generated by the CI; we keep only our patches in source control
android/
ios/
.gradle/
build/
local.properties
